# 에이전트 작업 규칙

주제별로 나뉜 여러 개의 정적 사이트를 담은 모노레포다.

## 사이트 구성

| 디렉토리 | 주제 | 담는 것 |
|---|---|---|
| `sites/photo` | 사진 | 증명사진 규격, GPS/EXIF 제거, 워터마크 |
| `sites/file` | 파일 변환 | 오디오 자르기, CSV 정리 |
| `sites/dev` | 개발자 도구 | CSS 단위, JSON, cron |

**사이트를 나눈 이유는 SEO다.** 검색엔진은 도메인이 "무엇에 관한 곳인지"로 신뢰도를
쌓는다. 사진 도구와 CSS 도구가 한 도메인에 섞이면 어느 쪽으로도 인정받지 못한다.
**주제가 다른 도구를 기존 사이트에 넣지 말 것.** 맞는 사이트가 없으면 이슈에 그렇게 적는다.

## 새 도구를 추가하는 표준 절차

작업 위치는 `sites/<site>/` 안이다. 아래 경로는 전부 그 기준이다.

1. **`src/lib/<name>.ts`** — 로직을 순수 함수로. DOM·브라우저 API 의존 금지.
   잘못된 입력은 예외를 던지거나 `null` 을 돌려주고, 무엇이 잘못됐는지 알린다.
2. **`src/lib/<name>.test.ts`** — Vitest. 정상 케이스, 경계값, 잘못된 입력을 모두 덮는다.
3. **`src/pages/tools/<slug>.astro`** — `Base` 레이아웃 사용.
   `<Base title description toolName>` 에 **`toolName` 을 반드시 넘긴다** (구조화 데이터가 달라진다).
   입력 요소에는 `<label for>` 를 붙인다 (접근성 + E2E 선택자).
4. **`src/tools/entries/<slug>.ts`** — 목록용 메타데이터. 목록은 이 디렉토리를 자동으로
   훑으므로 **`index.astro` 는 건드리지 않는다.**
5. **`e2e/<slug>.spec.ts`** — Playwright. 빌드 산출물 대상.
6. 페이지 하단에 **주제 설명 문단 2개 이상.**

`sites/photo/src/lib/id-photo.ts` 와 그 페이지가 기준 예시다.

## SEO — 선택이 아니라 기본이다

검색 유입이 유일한 유통 경로다. 다음은 반드시 지킨다.

- **`<h1>` 은 페이지당 하나.** 사용자가 검색할 표현을 그대로 쓴다
  (예: "cron 표현식 해석기" 보다 "cron 표현식 읽는 법" 이 검색어에 가깝다면 후자).
- **meta description 은 도구가 무엇을 해주는지 한 문장으로.** 검색 결과에 그대로 노출된다.
- **설명 문단은 실제 정보를 담는다.** 분량 채우기용 문장 금지 —
  구글의 scaled content abuse 정책에 걸린다.
- canonical, sitemap, robots.txt, 구조화 데이터는 레이아웃과 빌드 설정이 자동 처리한다.
  **`Base` 를 쓰지 않고 직접 `<html>` 을 쓰면 이게 전부 빠진다. 반드시 `Base` 를 쓸 것.**

## 파일을 다루는 도구의 검증

Canvas, File API, WebCodecs 를 쓰는 도구는 로직 전체를 순수 함수로 만들 수 없다.

- **`src/lib` (Vitest)** — 크기·비율 계산, 파라미터 결정, 파일명 생성, 입력 검증,
  바이트열 생성. 브라우저 없이 돌고 경계 조건이 많아 버그가 숨는 곳이다.
- **`e2e` (Playwright)** — 실제 변환 동작. `test/fixtures/` 에 작은 샘플을 두고
  업로드 → 처리 → 다운로드까지 확인하고, 결과의 크기·형식까지 단언한다.

**파일을 서버로 보내지 않는다.** 모든 처리는 브라우저 안에서 끝나야 한다.
이게 차별점이고 서버 비용을 0 으로 유지하는 방법이다.

## 검증 원칙

**"코드를 작성했다" 는 완료가 아니다.** PR 본문에 첨부한다:

- `npm run typecheck` 출력
- `npm run test:run` 출력

실행하지 못한 항목은 **"미검증"** 이라고 명시한다. 추측으로 완료 보고하지 않는다.

## 코드 규칙

- TypeScript strict. `any` 금지
- **새 의존성 추가 금지.** 필요하면 이슈에 먼저 제안한다 — 번들 크기가 곧 SEO 비용이다
- 스타일은 `Base.astro` 의 CSS 변수(`--fg`, `--bg`, `--border`, `--accent`)를 쓴다
- 모든 계산은 브라우저 안에서. 사용자 입력을 서버로 보내지 않는다

## 금지

- 프로덕션 배포, 파괴적 git (`push --force`, `reset --hard`, `clean -fdx`)
- `.env`, 키, 자격증명 파일 접근
- 사람 승인 없는 머지
- 지시받은 범위 밖의 파일 수정. 벗어나야 하면 중단하고 `TASK_BLOCKED` 을 출력한다

## 동시 작업 시 지켜야 할 것

여러 에이전트가 병렬로 작업한다. **공유 파일을 수정하면 충돌이 난다.**

새 도구를 추가할 때 만드는 파일은 전부 그 도구 전용이다. **기존 파일은 수정하지 않는다.**
공유 파일(`index.astro`, `Base.astro`, `site.ts`, `registry.ts`, 설정 파일)을 꼭 고쳐야
하는 상황이면 멈추고 이슈에 그 이유를 남긴다.

## 환경

- Windows 로컬 / Ubuntu CI. 셸은 PowerShell 5.1 (`&&`, `||` 없음)
- **한글이 든 `.ps1` 은 UTF-8 BOM 으로 저장할 것.** BOM 이 없으면 PowerShell 5.1 이
  CP949 로 읽어 엉뚱한 위치에서 파서 오류가 난다
- Node 24 / npm workspaces. 설치는 루트에서 `npm ci` 한 번
